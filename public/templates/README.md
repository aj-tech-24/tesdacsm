# Excel Templates

Formatted export has been removed.

Only raw export is active:

- Endpoint: `/api/admin/export-feedback-raw`

This folder is currently unused and kept only as a placeholder for future template-based export.
